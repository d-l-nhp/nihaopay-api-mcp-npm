// Docs content versioning is independent of package.json's npm version — this is the only
// source of truth for which nihaopay-api-docs release `pnpm fetch-docs` targets by default.
export const DEFAULT_DOCS_TAG = "v1.2.0";
